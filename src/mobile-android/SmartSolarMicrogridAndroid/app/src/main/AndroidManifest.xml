<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Ssmts">

        <!-- Member 3: Slot Selection (Launcher) -->
        <activity
            android:name="com.sliit.ssmts.reservation_workflow.ui.booking.SlotSelectionActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Member 3: Create Reservation Form -->
        <activity
            android:name="com.sliit.ssmts.reservation_workflow.ui.booking.CreateReservationActivity"
            android:exported="false" />

        <!-- Member 3: Booking Summary Receipt -->
        <activity
            android:name="com.sliit.ssmts.reservation_workflow.ui.summary.BookingSummaryActivity"
            android:exported="false" />

    </application>

</manifest>
