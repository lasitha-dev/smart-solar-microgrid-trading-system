<?xml version="1.0" encoding="utf-8"?>
<!--

  Description: Central Android application manifest defining required permissions (Internet, Camera),
               hardware feature specifications, and application configurations.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access for central C# Web API communication -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Camera hardware access for native QR scanning viewfinder -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera hardware features are not strictly required so tablets without rear cameras can still run the app -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@android:drawable/sym_def_app_icon"
        android:label="@string/app_name"
        android:roundIcon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ssmts.OperatorDashboard">

        <!-- Booking History, Search & Filter Activity (FR-M4-03) -->
        <activity
            android:name=".ui.history.BookingHistoryActivity"
            android:exported="false"
            android:label="@string/title_booking_history" />

        <!-- Operator Scanner & Verification Activity (FR-M4-05.2) -->
        <activity
            android:name=".ui.operator.OperatorScannerActivity"
            android:exported="false"
            android:label="@string/title_operator_scanner" />

    </application>

</manifest>
